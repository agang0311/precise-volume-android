plugins {
    id "com.android.application"
}

android {
    namespace "com.example.precisevolume"
    compileSdk 35

    defaultConfig {
        applicationId "com.example.precisevolume"
        minSdk 23
        targetSdk 35
        versionCode 1
        versionName "0.1.0"
    }

    buildTypes {
        release {
            signingConfig signingConfigs.debug
            minifyEnabled false
        }
    }
}
